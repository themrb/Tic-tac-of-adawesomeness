
package body Players is

   task body Player is
   begin
      accept Initialise;

      loop
         accept Next_Move(previous : in State_Type, next : out Place)

         end Next_Move;
      end loop;
   end Player

end Players;
