package body MinMax is

   function Min (current : in State) return BoardValue is
   begin
      return 0;
   end Min;

   function Max (current : in State) return BoardValue is
   begin
      return 0;
   end Max;

   function Eval(current : in State) return BoardValue is
   begin
      return 0;
   end Eval;

end MinMax;
